// Styles
import '../Styles/Photo.css'

function Photo(props) {
    const { name, description, camera, filmStock, image } = props;

    return (
        <div className='Photo'>
            
        </div>
    )
}

export default Photo;